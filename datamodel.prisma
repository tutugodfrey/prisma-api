type User {
  id: ID! @id
  name: String!
  posts: [Post!]! @relation(link: INLINE)
  email: String @unique
}

type Post {
  id: ID! @id
  title: String!
  published: Boolean! @default(value: false)
  author: User
}
